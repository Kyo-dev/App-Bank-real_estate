import 'package:flutter/material.dart';

class MapGeolocation extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('soy el mapa')
      
    );
  }
}